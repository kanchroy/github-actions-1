name: first-action

on: 
   push:
    branches:
        - main

jobs:
    first_job:
        runs-on: ubuntu-latest

        steps:
        - name: Check out Repo
          uses: actions/checkout@v6


        - name: list and display files
          run: |
            ls -al
            cat README.md

