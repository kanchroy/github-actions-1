name: first-action

on: 
   push:
    branches:
        - main

jobs:
    first_job:
        runs-on: ubuntu-latest

        steps:
        - name: Check out Repo
          uses: actions/checkout@v3


        - name: list files
          run: ls -al

        - name: Display Content
          run: cat README.md